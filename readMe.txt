Pour m'envoyer le fichier tu vas :
- d'abord entre dans le dossier CRUD_PHP_SOUTENANCE;
- copier le projet avec la base de donnees MySQL dans le dossier;
- ouvrir le terminale (CMD) depuis la barre de navigation;
- executer les commande suivante dans ton terminale(CMD):
> git add *
> git commit -m "Projet DRAG"
> git push